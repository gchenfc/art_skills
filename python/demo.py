import numpy as np

points = np.array([])
lines = np.array([points])
smooth_lines = np.array([])
drawing = False
showSmoothed =  False


def draw():
    for i < len(lines)
        for j < len(lines(i)-1)
            if drawing and i == len(lines) - 1
                # set color to cyan, 0 255 255
            if showSmoothed
                if (i < len(smooth_lines))
                




#{
#float* smoother;
#vector<vector<ofPoint>> lines;
#vector<vector<ofPoint>> smooth_lines;
#vector<vector<ofPoint>> curvature;
#vector<vector<float>> velocity;
#vector<vector<ofPoint>> salient_points;
#vector<vector<long long>> timestamps;
#bool drawing = false;
#bool showSmoothed = false; 
# }